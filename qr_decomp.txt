================
QR Decomposition
================

Basic Understanding
===================

What is QR decomposition?
-------------------------

Any matrix :math:`A` can be decomposed as a product of an *orthonormal matrix*
:math:`Q` and an upper triangular matrix :math:`R`.

.. math::

   A = Q R


...and what is an orthonormal matrix?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

:math:`Q` is *orthonormal* if it preserves dot products after vector
transformation.  That is,

.. math::

   Q \mathbf{u} \cdot Q \mathbf{v} = \mathbf{u} \cdot \mathbf{v}

or using transpose operations,

.. math::

   \mathbf{u}^T Q^T Q \mathbf{v} = \mathbf{u}^T \mathbf{v}.

   \Rightarrow \mathbf{u}^T \left(Q^T Q - I\right) \mathbf{v} = 0

Since :math:`\mathbf{u}` and :math:`\mathbf{v}` are arbitrary, :math:`Q^T Q =
I` and we get the orthogonality property,

.. math::

   Q^{-1} = Q^T.

The most common example of an orthogonal matrix is the rotation matrix,

.. math::

   R(\theta) = \begin{pmatrix}
      \cos \theta & \sin \theta \\
      -\sin \theta & \cos \theta
   \end{pmatrix}

which rotates a vector by angle :math:`\theta`.

Another is the reflection matrix,

.. math::

   r(\theta) = \begin{pmatrix}
      \cos \theta & \sin \theta \\
      \sin \theta & -\cos \theta
   \end{pmatrix}

which reflects a vector along a line of angle :math:`\theta / 2`.  Both are
important tools for construction of :math:`Q`.

The most interesting property of the orthogonal matrix is the *orthogonality*
of its column vectors.  If :math:`Q = ( \mathbf{q}_1, \mathbf{q}_2,
\mathbf{q}_3, \ldots )`, then

.. math::

   Q^T Q &=
      \begin{pmatrix}
         \mathbf{q}_1 \\ \mathbf{q}_2 \\ \mathbf{q}_3 \\ \vdots
      \end{pmatrix}
      \begin{pmatrix}
         \mathbf{q}_1 & \mathbf{q}_2 & \mathbf{q}_3 & \cdots
      \end{pmatrix}

   &=
      \begin{pmatrix}
         \mathbf{q}_1^T \mathbf{q}_1
         & \mathbf{q}_1^T \mathbf{q}_2
         & \mathbf{q}_1^T \mathbf{q}_3
         \cdots \\
         \mathbf{q}_2^T \mathbf{q}_1
         & \mathbf{q}_2^T \mathbf{q}_2
         & \mathbf{q}_2^T \mathbf{q}_3
         & \cdots \\
         \mathbf{q}_3^T \mathbf{q}_1
         & \mathbf{q}_3^T \mathbf{q}_2
         & \mathbf{q}_3^T \mathbf{q}_3
         & \cdots \\
         \vdots & & & \ddots
      \end{pmatrix}

Since :math:`Q^T Q = I`, it must be that :math:`\mathbf{q}_i^T \mathbf{q}_j =
\delta_{ij}`.  That is, the column vectors of :math:`Q` form an orthogonal
basis.


Why would you do this?
----------------------

If you know :math:`Q` and `R`, then you can solve systems of equations without
calculating an inverse operator.

.. math::

   A \mathbf{x} = Q R \mathbf{x} = \mathbf{b}

   \Rightarrow R \mathbf{x} = Q^{-1} \mathbf{b} = Q^T \mathbf{b}

Since :math:`R` is upper triangular, it's just *back substitution* to get
:math:`\mathbf{x}`.

(Nearly orthogonal decomposition is motivated by this result, e.g.
:math:`e^{\pm ix}` in Fourier analysis.)

Other uses (to be explained below)

* Solving an overdetermined system based on minimizing :math:`|Ax - b|`.

* Computing the eigenvalues of :math:`A` ("QR algorithm")

* :math:`Q` provides a way to change the basis of :math:`\mathbf{x}` (although
  I don't know how useful this is unless you have some control over :math:`Q`)



What is the shape of Q and R if the input matrix is m×n with m≥n?
-----------------------------------------------------------------


How does QR decomposition differ from LU decomposition?
-------------------------------------------------------


Can all matrices be decomposed using QR?
----------------------------------------


Conceptual & Theoretical
========================

Is the QR decomposition unique? Under what conditions?

How is the QR decomposition related to the Gram-Schmidt process?

Why is the Q matrix orthogonal (or unitary)?

Why might you use QR decomposition instead of LU for solving linear systems?

Explain the role of QR decomposition in solving least squares problems.


Computational/Algorithmic
=========================

How do you compute QR decomposition using Gram-Schmidt?

What is the difference between classical and modified Gram-Schmidt?

Describe how Householder reflections are used in QR decomposition.

What are Givens rotations, and how are they applied in QR decomposition?

Which method is numerically more stable: Gram-Schmidt or Householder? Why?


Applications
============

How is QR decomposition used in solving overdetermined systems?

Explain how QR decomposition is used in eigenvalue algorithms (e.g., QR algorithm).

Describe a practical scenario where QR decomposition would be preferable to SVD.
